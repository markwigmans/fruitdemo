spring.application.name=frontend
spring.cloud.config.uri=http://${CONFIG_HOST:localhost}:${CONFIG_PORT:8888}
spring.cloud.config.fail-fast=true
spring.cloud.config.retry.maxAttempts=${MAX_RETRIES:1000000}
